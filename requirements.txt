# --- Çekirdek ---
pydantic==2.9.2
pyyaml==6.0.2
python-dotenv==1.0.1

# --- Toplama (Katman 0) ---
httpx==0.27.2          # robots.txt kapisi
trafilatura==1.12.2    # govde ayiklama
selectolax==0.3.27     # HTML ayristirma (cikarim katmani)
selenium==4.27.1       # kampanya listeleri JS ile render ediliyor
webdriver-manager==4.0.2   # chromedriver indirimi; yoksa Selenium Manager devreye girer

# --- Depolama (Katman 3) ---
SQLAlchemy==2.0.36

# --- Zekâ (Katman 4) ---
numpy==2.1.3
ollama==0.6.2          # >=0.5 şart: `think` parametresi bu sürümle geldi
openai==1.55.0         # EVREN gömme ucu (OpenAI uyumlu /v1/embeddings)

# --- Sunum (Katman 5) ---
streamlit==1.40.1
pandas==2.2.3
plotly==5.24.1
fastapi==0.115.5
uvicorn==0.32.1

# --- Sunum ciktisi (yalniz `make sunum-pptx`) ---
python-pptx==1.0.2     # MIT — PPTX yazimi
pypdfium2==4.30.0      # Apache-2.0/BSD-3 — PDF sayfasini goruntuye cevirir
                       # (PyMuPDF AGPL-3.0 oldugu icin BILEREK kullanilmiyor)

# --- Geliştirme / test ---
pytest==8.3.3
ruff==0.7.4
pip-licenses==5.0.0
